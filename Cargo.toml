[package]
name = "zig_tool"
version = "0.12.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1.4.1"
proto_pdk = "0.34.0"
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
proto_pdk_test_utils = "0.48.0"
starbase_sandbox = "0.12.0"
tokio = { version = "1.52.3", features = ["full"] }

[features]
default = ["wasm"]
wasm = []

[profile.release]
codegen-units = 1
debug = false
lto = true
opt-level = "s"
panic = "abort"
